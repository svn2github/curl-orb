|||
||| Curl ORB for java (version 0.8)
|||  This code was generated by the Curl code generator automatically.

|||  package name   : $package_name
|||  generated date : $generate_date
|||

#foreach ($import_package in $import_packages)
{import * from $import_package}
#end

#docstring ( $classJavadoc, true )
{define-class public $class_name 
#if ($has_superclass)
  {inherits $superclass_name}
#end

#foreach ($field in $fields)
#docstring ( $field.javadocContent, false )
#if ($field.isTransient)
  field transient $field.getterModifier $field.setterModifier $field.name:$field.type $field.comment
#else
  field $field.getterModifier $field.setterModifier $field.name:$field.type $field.comment
#end
#end

  {constructor public {default message:String}
    {construct-super message}
  }
}
